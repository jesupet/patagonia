import { createStore } from 'vuex'
import preguntas from './preguntas.json'


export default createStore({
  state: {
    preguntas: preguntas,
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
